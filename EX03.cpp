#include<stdio.h> // Biblioteca que contém funções para entrada e saída
#include<locale.h> // Biblioteca para definir o idioma do programa para Português
#include<string.h> // Biblioteca para manipular várias funções de strings
#include<stdlib.h> // Biblioteca que contém funções e constantes básicas

int main(){ // início do programa
	setlocale(LC_ALL,"portuguese"); // definir idioma para português
	
	float altura1, altura2, altura3, altura4, altura5, altura6, altura7, altura8, altura9, altura10;
		
		
		printf("Digite a altura da pessoa: ");
		scanf("%f", &altura1);
		while((altura1<0,50)or(altura1>2,90)){
			printf("altura inválida, digite uma altura válida: ");
			scanf("%f", &altura1);
}
		printf("Digite a altura da pessoa: ");
		scanf("%f", &altura2);
		while((altura2<0,50)or(altura2>2,90)){
			printf("altura inválida, digite uma altura válida: ");
			scanf("%f", &altura2);
}
		printf("Digite a altura da pessoa: ");
		scanf("%f", &altura3);
		while((altura3<0,50)or(altura3>2,90)){
			printf("altura inválida, digite uma altura válida: ");
			scanf("%f", &altura3);
}
		printf("Digite a altura da pessoa: ");
		scanf("%f", &altura4);
		while((altura4<0,50)or(altura4>2,90)){
			printf("altura inválida, digite uma altura válida: ");
			scanf("%f", &altura4);
}
		printf("Digite a altura da pessoa: ");
		scanf("%f", &altura5);
		while((altura5<0,50)or(altura5>2,90)){
			printf("altura inválida, digite uma altura válida: ");
			scanf("%f", &altura5);
}
		printf("Digite a altura da pessoa: ");
		scanf("%f", &altura6);
		while((altura6<0,50)or(altura6>2,90)){
			printf("altura inválida, digite uma altura válida: ");
			scanf("%f", &altura6);
}
		printf("Digite a altura da pessoa: ");
		scanf("%f", &altura7);
		while((altura7<0,50)or(altura7>2,90)){
			printf("altura inválida, digite uma altura válida: ");
			scanf("%f", &altura7);
}
		printf("Digite a altura da pessoa: ");
		scanf("%f", &altura8);
		while((altura8<0,50)or(altura8>2,90)){
			printf("altura inválida, digite uma altura válida: ");
			scanf("%f", &altura8);
}
		printf("Digite a altura da pessoa: ");
		scanf("%f", &altura9);
		while((altura9<0,50)or(altura9>2,90)){
			printf("altura inválida, digite uma altura válida: ");
			scanf("%f", &altura9);
}
		printf("Digite a altura da pessoa: ");
		scanf("%f", &altura10);
		while((altura10<0,50)or(altura10>2,90)){
			printf("altura inválida, digite uma altura válida: ");
			scanf("%f", &altura10);
}
		
		
		return 0;
		
		
}
